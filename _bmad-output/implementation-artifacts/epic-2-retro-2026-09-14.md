---
epic: 2
date: 2026-09-14
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 2 retrospective — Get escalating frost warnings I can silence

## Epic summary

- **Epic:** 2 — Get escalating frost warnings I can silence
- **Diff range:** `b002227..61725e5` (parent of first story commit `79a1659` through HEAD merge `61725e5`)
- **Stories completed:** `2-1-classify-frost-risk-and-remaining-time-window`, `2-2-fetch-hourly-forecast-from-open-meteo`, `2-3-send-a-preview-first-telegram-frost-alert`, `2-4-flip-season-from-telegram`, `2-5-run-a-local-check-tick`
- **Unfinished stories (`pending_stories`):** none
- **Sprint retro key:** `epic-2-retrospective` was `optional` at gather time
- **Declared criteria:** Epic 2 FRs FR5, FR6, FR7, FR8, FR9, FR10, FR11, FR12, FR13, FR21 plus per-story ACs in `_bmad-output/planning-artifacts/epics.md` L248–399

### Evidence inventory

| Artifact | Status | Source |
|---|---|---|
| Epic spec / ACs | available | `_bmad-output/planning-artifacts/epics.md` L150–152, L248–399; `_bmad-output/implementation-artifacts/epic-2-context.md` |
| Story files | available | `spec-2-1-*.md` … `spec-2-5-*.md` (all `status: done`) |
| Diff range + git_evidence | available | `git_evidence.py --range b002227..61725e5`; 15 commits, 5 merges, `merges_measured` = `merge_count` = 5, `binary_revisions` = 0 |
| Sprint status | available | `_bmad-output/implementation-artifacts/sprint-status.yaml`; five story keys `done`; `epic-2` still `in-progress`; retro key `optional` |
| Architecture | available | `ARCHITECTURE-SPINE.md` AD-1, AD-3, AD-5, AD-6 (local slice), AD-7, AD-8, AD-9, AD-11 (Open-Meteo only), AD-12, AD-13 (through StateStore write) |
| Previous retrospective | available | `_bmad-output/implementation-artifacts/epic-1-retro-2026-09-13.md`; three open `action_items` in sprint-status |
| Session logs | available | [2.1 classify](1a517e6b-68e7-4a07-90fe-e7197b210251); [2.2 forecast](51d0ea85-c113-4117-8929-1f1e20f2dbb1); [2.3 send](bc49ba67-253d-44fc-b780-619cc9a04641); [2.4 season](349a8b6c-c7cd-4e30-aa42-95a266b79f7c); [2.5 tick](e434e9a6-1da1-4bef-bc22-4b72c943a9cd) |
| Behavior check | done this run | `uv run pytest` 171 passed; live Open-Meteo + classify; CLI fail-closed. No live Telegram |

### git_evidence notes

- `files` ranks tests first: `tests/test_check_tick.py` +753, `tests/test_season_flip.py` +567, `tests/test_frost_alert_send.py` +349, `tests/test_open_meteo_forecast.py` +224, `tests/test_classify.py` +191.
- Production max: `telegram_ack_inbox.py` +144, `telegram_notifier.py` +113, `open_meteo_forecast.py` +95, `check.py` +70, `classify.py` +67, `check_tick.py` +59.
- Per-story `stories` on **feat** commits is populated for all five (`79a1659`, `1a8e7ba`, `3625986`, `b214684`, `36e78b6`). Epic 1 action item 3 landed on this range. `flip story to done` subjects still have `"stories": []`.
- `merge_files` restates the same churn; no merge-only conflict paths. `binary_revisions` 0.

## Findings

### Aggregate views

**Architecture delta** (import graph over `src/frost_alert`; no pydeps). Domain modules import stdlib only (`classify.py`, `season.py`, `alert_windows.py`). HTTP/`urllib` only in adapters (`open_meteo_forecast.py`, `telegram_notifier.py`, `telegram_ack_inbox.py`, plus Epic 1 `open_meteo_geocoder.py`). No adapter→adapter imports. No domain→adapters. Geocode remains setup-only (`setup.py:8,45–47`); check uses stored lat/lon (`check_tick.py:26`). `Watchdog` still `pass` (`ports/__init__.py:27–28`). Ports now import domain types (`ports/__init__.py:4`). Package-root composers `check_tick.py`, `send_frost_alert.py`, `apply_season.py` sit outside AD-12’s four folders — named in story Code Maps; 2.5 review already accepted.

**Duplication map.** Four adapters copy `USER_AGENT` + `TIMEOUT_S=15` + inline `urlopen`. `_is_number` is duplicated in geocoder and forecast (`open_meteo_forecast.py:94–95`). One `classify()`, one `apply_intent()`.

**God-class / size growth.** `git_evidence.py` `files` ranks tests first (`test_check_tick.py` +753). Production tops: `telegram_ack_inbox.py` 144 lines (Telegram intake only), `telegram_notifier.py` 113 (send + copy), `open_meteo_forecast.py` 95 (fetch + parse). Opened those files: single-purpose, not god-classes. `binary_revisions` 0; `merges_measured` = `merge_count` 5.

**Pattern divergence vs Epic 1.** ConfigStore gained `load` (`json_config_store.py:35–37`). StateStore gained `write` (`json_state_store.py:24–41`). Clock gained `now` + `SystemClock`. `except Exception` added at `check_tick.py:50–51` (send continue) and `check.py:58–59` (tick abort). Composers moved to package root.

**Spec-to-implementation.**

| Criterion | Evidence | Call |
|---|---|---|
| FR5 any-hour 30h lookahead | `classify.py:24,37–40`; `tests/test_classify.py:55–110` | met |
| FR6 forecast hours only; no live temp; daytime counts | `open_meteo_forecast.py:27–28`; `test_open_meteo_forecast.py:110` no `current_weather`; `test_classify.py:74–85` | met |
| FR7 windows 24/12/6 once per event | `classify.py:54–59`; `alert_windows.py:8–21`; `send_frost_alert.py:21–33` | met |
| FR8 local event date; same-day no reset | `classify.py:60`; `check_tick.py:33–37`; `test_classify.py:132–139` | met |
| FR9 no alert if absent or suspended | `check_tick.py:25–51`; `test_check_tick.py:278–330` | met |
| FR10 preview-first | first line `telegram_notifier.py:87`; facts on later lines `91–92`; `test_frost_alert_send.py:244–258` | met at first-line split; see finding 7 |
| FR11 `/in` / `plants_in` suspends, keeps event/windows | `season.py:13–14`; `test_season_flip.py:187–200` | met |
| FR12 `/out` clears; no snooze | `season.py:15–16`; no snooze keys in `json_state_store.py:34–40` | met |
| FR13 Telegram-only; answer callbacks; ignore other chats | `telegram_ack_inbox.py:27–30,102–104`; `test_season_flip.py:288–300,557–567` | met |
| FR21 `getUpdates` offset+1; no webhook | `telegram_ack_inbox.py:42–43`; `test_season_flip.py:209,213`; `setWebhook` absent in `src/` | met |
| NFR3 secrets from env | `check.py:27–30` | met |
| NFR6 no `models=` pin | `open_meteo_forecast.py:23–29`; `test_open_meteo_forecast.py:109` | met |
| NFR11 fake ports | socket-blocked tests; 171 passed | met |

### Diff-scope review (`bmad-review` on `b002227..61725e5`)

Lenses: adversarial, edge-case-hunter, verification-gap. Weight: story boundaries. Independent subagents; findings re-checked against primary source before keep.

Kept after source re-check:

1. **Four urllib adapters, no shared HTTP helper.** Source: `USER_AGENT`/`TIMEOUT_S` in forecast, notifier, ack inbox, geocoder. **Defer** to 3.2 (MET Norway would be a fifth copy). Process: extract before failover lands.
2. **Package-root composers vs AD-12 four folders.** Source: `check_tick.py`, `send_frost_alert.py`, `apply_season.py`; spine L106–110. Story Code Maps named the paths; 2.5 review `false`. **Accept as-is.**
3. **`except Exception` around send.** Source: `check_tick.py:50–51`; 2.5 Frozen Always + `test_check_tick.py:356–377`. **Accept as-is.**
4. **Poll raise after successful send does not persist the mark.** Source: `check.py:49–59`; `test_check_tick.py:400–417`; 2.5 matrix “Poll raise \| no write”. Duplicate alert next tick is specified. **Accept as-is.**
5. **Present `data/state.json` is still unvalidated.** Source: `json_state_store.py:22`; Epic 1 deferred to 3.3. `check.py` now catches corrupt JSON (`check.py:37–38`) but not missing keys. **Defer** to 3.3.
6. **NaN/Inf temperatures pass `_is_number`.** Source: `open_meteo_forecast.py:94–95`; reproduced this run: `_is_number(nan)` True, `classify` → `risk_present=True` window 24 (`nan > threshold` is False). **Defer** to 3.2 malformed→failover — named open item, not a silent nice-to-have (Mary).
7. **FR10 location sits on line 2 of a single `sendMessage` text.** Source: `telegram_notifier.py:87–92`; tests only assert first line (`test_frost_alert_send.py:244–258`). Telegram has no separate preview field. **Accept as-is**; lock-screen leak stays a logged open question (Mary/Sally), not a spec miss (John).
8. **Whitespace-only secrets: guard present, test absent.** Source: `check.py:29` `.strip()`; `test_check_tick.py` covers unset only (2.5 triage claimed a patch). **Defer** coverage; not a missing guard.
9. **Monitoring + `/out` same tick is untested.** Source: `check_tick.py:40–58` send-then-poll; `test_matrix_suspended_polls_and_writes` uses `season=suspended`. Product is FR12 (clear so resume can warn). **Accept as-is** product; coverage gap is process.
10. **Foreign-chat `answerCallbackQuery` failure aborts the owner batch.** Source: `telegram_ack_inbox.py:27–30`; 2.4 Always answers every callback. **Accept as-is.**
11. **No `deleteWebhook`.** Source: `src/` has no webhook calls; 2.4 triage accepted. **Accept as-is.**

Dropped after source re-check: new `event_date` persisted on send fail (2.5 Always resets windows *before* send; retry is the point); `/out` while monitoring (FR12); missing `plants_out` on frost keyboard (FR10 ack is `plants_in`); `mark_window` duplicates (`should_send` membership); write `OSError` after poll (2.5 rejected); missing config keys skipping poll (fail-closed is specified).

Edge-case lens (4): same-tick `/out` folded into #9; non-list `alerted_windows` folded into #5; NaN kept as #6; missing `update_id` **accept as-is** (Telegram always sends it).

Verification-gap lens (2): whitespace tests = #8; monitoring+`/out` = #9.

### Wins the evidence confirms

- 171 pytest, 1.23s, sockets blocked.
- Feat subjects name story slugs (`79a1659`, `1a8e7ba`, `3625986`, `b214684`, `36e78b6`) — Epic 1 action item 3 landed.
- Live Open-Meteo Berlin fetch: 168 hourly UTC-aware hours, 3600s steps; classify at threshold 3°C → `risk_present=False` (lookahead min 11.6°C on 2026-09-14).
- PRs #4–#8 from story branches, not `master`.
- Tick matrix rows in `test_check_tick.py` cover send, skip, suspended, new event, notifier raise, forecast raise, poll raise, missing config/secrets.

### Team discussion (Phase 3, yolo / non-interactive)

Ran `bmad-party-mode` with independent subagents (Mary, John, Sally, Winston, Amelia party-voice) over the sourced findings. Not session-inline. Unanimous machine vote: **accepted-with-open-items**.

Clash that earned its keep: Mary refused to treat FR10 line-2 location as a footnote — log it as an open item — and refused to defer NaN as a 3.2 nice-to-have (`classify` treats nan as frost). John: FR10 line 1 matches `epics.md` L322 / `alert-copy.md` L9; lock-screen is a client question, not a product hole; carry Epic 1 items 1–2. Sally: the nightstand glance is `❄️ Frost risk in ~24h`, not Stockholm; she will not call a spec miss and will not close the client question. Winston: extract HTTP before MET Norway or inherit a fifth copy; AD-4 `check.yml` deferral still must land in `epic-1-context.md` / `deferred-work.md` before Epic 3 forks the spine. Amelia: HTTP helper is 3.2 Always, not a separate remediation; `except Exception` and poll-raise-no-write are Frozen Always with tests; NaN tightens under 3.2 malformed-hourly failover; 2.5 whitespace-test patch never landed in `test_check_tick.py`. No new finding survived source re-check.

## Behavior verification

- `uv run pytest` — 171 passed (1.23s). Sockets blocked in unit tests.
- `uv run frost-alert` (no args) — exit 1, stderr `Usage: frost-alert setup|check`.
- `uv run frost-alert check` with empty `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` — exit 1, no `data/state.json` written.
- Live `OpenMeteoForecast().fetch(lat=52.52437, lon=13.41053)` — 168 hours, UTC-aware, 3600s steps; `classify(..., 3, now, "Europe/Berlin")` → absent risk (lookahead 11.6–24.5°C on 2026-09-14).
- NaN probe (not a product path): `_is_number(nan)` True; `classify` of a NaN hour → `risk_present=True`, window 24.
- Live Telegram send **not** exercised — no bot token/chat id in the process environment (NFR3).

## Previous-retro follow-through

Prior retro: `_bmad-output/implementation-artifacts/epic-1-retro-2026-09-13.md`. `sprint-status.yaml` `action_items` has three Epic 1 entries, all `open`.

| id | Landed? | Evidence | Status this run would propose |
|---|---|---|---|
| `epic-1-retro-item-1-reconcile-story-1-2-verification-with-1` | no evidence found | `spec-1-2-set-scale-and-threshold-through-setup.md:118` still says redirected empty answers write `config/user.json` | (none — still open) |
| `epic-1-retro-item-2-update-epic-1-context-md-so-check-yml-se` | no evidence found | `epic-1-context.md:29` still names `.github/workflows/check.yml` as a structural seed | (none — still open) |
| `epic-1-retro-item-3-include-the-story-slug-in-feat-commit-su` | landed | feat subjects `79a1659`, `1a8e7ba`, `3625986`, `b214684`, `36e78b6` name the story slug; `git_evidence.py` `stories` arrays populated | `done` |

Headless: do not pass `--set-action-status`. Item 3 transition is recorded here only.

## Action items

Proposed, not applied. Owners as named. Fixes and spec edits wait for Gicki.

1. **Extract a shared stdlib HTTP helper in Story 3.2 before adding MET Norway.** Four adapters copy `USER_AGENT` / `TIMEOUT_S=15` / `urlopen`. Owner: Amelia. Kind: process (3.2 Always, not a separate code patch from this retro).
2. **Reject NaN/Inf hourly temperatures as malformed in Story 3.2.** `_is_number` at `open_meteo_forecast.py:94–95` accepts them; `classify` then treats NaN as frost. Owner: Amelia. Kind: remediation scoped to 3.2 malformed→failover (`epics.md` Story 3.2).

Not duplicated as retro items: present-file `state.json` validation → 3.3 (already Epic 1 deferral); `check.yml` seed → 3.1 (Epic 1 item 2 still open); whitespace-secret tests (guard exists); FR10 lock-screen (open question, no code change without a Bot API the spec never named).

## Acceptance verdict

**accepted-with-open-items.** Criteria **declared** (epics.md Epic 2 FRs + story ACs). `pending_stories` empty, so unfinished-story fail-safe did not fire. No human override (yolo).

Evidence: FR5–FR13 and FR21 held in tests, in a live Berlin forecast classify (absent risk), and in CLI fail-closed paths. Named open items above are 3.2 process/hardening plus carried Epic 1 spec reconciliations, not blocking product holes. Party unanimous on this spelling.

## Open questions

- Does a real Telegram Android/iOS lock-screen preview show only line 1 of `sendMessage` text, or does `place_name` on line 2 leak? Spec and tests pin the first-line split (`telegram_notifier.py:87–92`; `test_frost_alert_send.py:244–258`). A human with a phone would close this; it does not change the epic verdict.
- Should whitespace-only secret tests land on the next `check.py` touch, or wait for Epic 3 secrets-in-Actions work?

## Assumptions

- Epic selected from invocation (`ER epic 2`), confirmed by `detect-epic --epic 2`: `story_count` 5, `pending_stories` `[]`, `retro_status` `optional`.
- Yolo treated as headless confirmations: no going-in-concerns prompt; no human verdict override prompt; sprint-status update runs without asking.
- Party requested explicitly, so Phase 3 ran despite the headless “never open the team discussion” default. Party was `--non-interactive` (yolo): one seeded round of independent subagents, then wrap. No HTML keepsake written.
- Criteria treated as **declared** (epics.md FR coverage + story ACs), not profiled.
- Machine verdict **accepted-with-open-items** recorded with no human decision at the console.
- Proposed action items listed above; none auto-applied to specs or code.
- Previous-retro `--set-action-status` for `epic-1-retro-item-3-include-the-story-slug-in-feat-commit-su` → `done` would have been proposed interactively; headless did not write it.
- Live Telegram send skipped (no secrets in env).
