---
epic: 1
date: 2026-09-13
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 1 retrospective — Configure frost watch without editing source

## Epic summary

- **Epic:** 1 — Configure frost watch without editing source
- **Diff range:** `14d213d..a086b30` (parent of first story commit `237b210` through HEAD merge `a086b30`)
- **Stories completed:** `1-1-installable-frost-alert-package`, `1-2-set-scale-and-threshold-through-setup`, `1-3-set-location-through-geocode-once-setup`
- **Unfinished stories (`pending_stories`):** none
- **Sprint retro key:** `epic-1-retrospective` was `optional` at gather time
- **Declared criteria:** Epic 1 FRs FR1, FR2, FR3, FR4, FR22 plus per-story ACs in `_bmad-output/planning-artifacts/epics.md` L158–246

### Evidence inventory

| Artifact | Status | Source |
|---|---|---|
| Epic spec / ACs | available | `_bmad-output/planning-artifacts/epics.md` L146–246; `_bmad-output/implementation-artifacts/epic-1-context.md` |
| Story files | available | `spec-1-1-*.md`, `spec-1-2-*.md`, `spec-1-3-*.md` (all `status: done`) |
| Diff range + git_evidence | available | `git_evidence.py --range 14d213d..a086b30`; 10 commits, 3 merges, `merges_measured` = `merge_count` = 3, `binary_revisions` = 0 |
| Sprint status | available | `_bmad-output/implementation-artifacts/sprint-status.yaml`; three story keys `done`; epic-1 still `in-progress` |
| Architecture | available | `ARCHITECTURE-SPINE.md` AD-1, AD-2, AD-3, AD-7, AD-12 (this epic); AD-4 workflow seed relevant as delta |
| Previous retrospective | missing | no `epic-*-retro-*.md`; sprint-status has no `action_items` key |
| Session logs | available | [1.1 package](85baa842-3bf7-4880-9564-451d2360610e); [1.2 setup](78dbdeea-967b-4a6c-a1dd-928af94ee1fe); [1.3 geocode](e6c0067e-538f-41a6-aa94-6a9d78e3e8d4) |
| Behavior check | done this run | live `frost-alert setup` Berlin confirm; pytest 55 passed |

### git_evidence notes

- `files` ranks test modules highest (`tests/test_setup_location.py` +567, `tests/test_setup_scale_threshold.py` +314 net). Production max is `open_meteo_geocoder.py` +87 and `setup.py` +84.
- Per-story `stories` on commits is empty for every non-merge feat (`237b210`, `c92e444`, `d30cb8b`) because subjects do not name story ids. Attribution for later views uses merge subjects plus session order, not the JSON `stories` arrays.
- `merge_files` restates the same churn; no merge-only conflict paths.

## Findings

### Aggregate views

**Architecture delta** (inline import graph over `src/frost_alert`; no pydeps). Domain `scale_threshold.py` imports only stdlib. HTTP lives in `adapters/open_meteo_geocoder.py`. `entrypoints/setup.py` wires `JsonConfigStore` + `OpenMeteoGeocoder`. No `.github/workflows/check.yml`. No cycles. ConfigStore is write-only; StateStore is load-only — methods added when first needed (1.1 Never: do not invent signatures).

**Duplication map.** `JsonConfigStore` and `JsonStateStore` share a CWD-root constructor. Geocoder is a third adapter shape (HTTP). Not a second implementation of the same problem.

**God-class / size growth.** `git_evidence.py` `files` ranks tests first (`test_setup_location.py` +567). Production tops: `open_meteo_geocoder.py` +87, `setup.py` +84. Opened those files: single-purpose modules, not god-classes. `binary_revisions` 0; `merges_measured` = `merge_count` 3.

**Pattern divergence.** 1.2 `write_scale_and_threshold` is gone (`rg` in `src/` is empty). 1.3 `write` seven keys is the only ConfigStore method. 1.2 CLI tests now feed Berlin + `FakeGeocoder` (`tests/test_setup_scale_threshold.py:137`). Domain isolation scanner was patched (`tests/test_package.py:131–195`).

**Spec-to-implementation.**

| Criterion | Evidence | Call |
|---|---|---|
| FR1 setup CLI scale/threshold/location | `setup.py:30–70`; live run | met |
| FR2 geocode once, confirm, keys from geocoder | live Berlin write; golden lat `52.52437` | met |
| FR3 never re-geocode on check path | no check entrypoint; `geocode` only in setup + geocoder adapter | vacuously met |
| FR4 default 3; F→C in domain | `scale_threshold.py:23–47`; tests | met |
| FR22 seven config keys; five state keys | `json_config_store.py:24–32`; `json_state_store.py:15–21` | met for this epic’s writers |
| Epic-context / starter `check.yml` seed | no `.github/` | **divergence** — stories forbade it |

### Diff-scope review (`bmad-review` on `14d213d..a086b30`)

Lenses: adversarial, edge-case-hunter, verification-gap. Weight: story boundaries.

Kept after source re-check:

1. **Missing `check.yml` seed.** Source: `epic-1-context.md:29`; architecture starter in `epics.md:103`; tree has no `.github/`. Stories 1.1/1.3 Never forbade creating it. **Defer** to 3.1. Process: compiled epic context was not updated when 1.1 froze the seed out.
2. **Feat commit subjects omit story ids.** Source: `git_evidence.py` JSON — `237b210`, `c92e444`, `d30cb8b` have `"stories": []`; only merge subjects match. **Process lesson.**
3. **Present `data/state.json` is unvalidated.** Source: `json_state_store.py:22`; `tests/test_state_store.py` covers missing-file only. 1.3 review already rejected this as unused. **Defer** to 3.3.
4. **1.2 spec Verification is stale.** Source: `spec-1-2-*.md` Verification: empty answers → `user.json`. 1.3 `setup.py:41–43` empty location exits 1 with no write. **Spec reconciliation (proposed).**
5. **`except Exception` around `geocode`.** Source: `setup.py:46–49`. 1.3 review: user-facing outcome is no-match. **Accept as-is.**
6. **Geocoder is not a named port.** Source: `setup.py:15–16`; spec 1.3 Always. **Accept as-is.**
7. **FakeConfigStore / FakeStateStore are empty.** Source: `tests/test_package.py:72–77`. Construction-only. **Accept as-is.**

Dropped: leftover `write_scale_and_threshold` callers (none in `src/`); “1.2 wipe location” (1.3 replaced the writer); untyped `GeocodePlace` after confirm (production adapter returns `GeocodePlace`); EOF / non-atomic write (prior reviews rejected).

Edge-case lens (3): present-file state (folded into #3); stdin EOF; `ConfigStore.write` OSError. Last two **accept as-is** (1.2/1.3 review: not everyday).

Verification-gap lens: `[]` — checked and clean. Default `OpenMeteoGeocoder` wiring is tested (`tests/test_setup_location.py:208–214`). Default CWD store is tested (`tests/test_setup_scale_threshold.py:298–303`).

### Wins the evidence confirms

- 55 pytest, 0.33s, sockets blocked.
- Live Open-Meteo hit for Berlin matched spec 1.3 Design Notes golden (`place_name` Berlin, `lat` 52.52437, `lon` 13.41053, `elevation_m` 74, `timezone` Europe/Berlin, `threshold_c` 3, `scale` C). File deleted after capture; not left in the tree.
- Relative-import isolation hole from 1.1 review is patched.
- PRs #1–#3 from story branches, not `master`.

### Team discussion (Phase 3, yolo / non-interactive)

Ran `bmad-party-mode` with independent subagents (Mary, John, Sally, Winston, Amelia party-voice) over the sourced findings. Not session-inline. Unanimous machine vote: **accepted-with-open-items**.

Clash that earned its keep: John called missing `check.yml` an FR14/3.1 miss, not an Epic 1 product hole. Winston refused to wave the seed — AD-4 still names the file; record the deferral so Epic 3 does not fork the spine. Mary sided with the clone-and-run job over CI. Amelia forbade inventing a Geocoder port or landing `check.yml` as retro remediation (frozen Never). Sally: empty location, geocoder exception, and a true no-hit share `No matching place found.` — specified in 1.3 (“same as no match”); `_confirmed` only accepts empty/`y`/`Y`, so `yes` is reject as the spec wrote it. No new finding survived source re-check.

## Behavior verification

- `uv run pytest` — 55 passed (package 8, location 26, scale/threshold 19, state 2).
- `uv run frost-alert` (no args) — exit 1, stderr `Usage: frost-alert setup`.
- Live `frost-alert setup` with empty scale, empty threshold, `Berlin`, empty confirm — exit 0, shown `Berlin, State of Berlin, Germany`, wrote seven-key `config/user.json` matching the Open-Meteo docs golden. Harness used `uv run --directory` so CWD was the repo; file was deleted after the capture. Story 1.3 notes said live setup was not run; this retro ran it.

## Previous-retro follow-through

No prior retrospective document. `sprint-status.yaml` has no `action_items` array. Nothing to follow through on.

## Action items

Proposed, not applied. Owners as named. Fixes and spec edits wait for Gicki.

1. **Reconcile Story 1.2 Verification with 1.3.** `spec-1-2-set-scale-and-threshold-through-setup.md` Verification still claims redirected empty answers write `config/user.json`. After 1.3, empty location exits 1 with no write (`setup.py:41–43`). Owner: Gicki. Kind: spec reconciliation.
2. **Point the compiled epic context at 3.1 for `check.yml`.** `epic-1-context.md:29` still says this epic leaves `.github/workflows/check.yml` as a structural seed. Stories froze that out; 3.1 owns the schedule. Owner: Gicki. Kind: spec reconciliation. Do not land a stub workflow from this retro.
3. **Put the story slug in feat commit subjects.** `237b210`, `c92e444`, `d30cb8b` left `git_evidence.py` `stories: []`. Merge subjects matched; feat subjects did not. Owner: Amelia. Kind: process.

Not duplicated as retro items (already backlog stories): present-file `state.json` validation → 3.3; workflow seed → 3.1.

## Acceptance verdict

**accepted-with-open-items.** Criteria **declared** (epics.md Epic 1 FRs + story ACs). `pending_stories` empty, so unfinished-story fail-safe did not fire. No human override (yolo).

Evidence: FR1–FR4 and FR22 held in tests and in a live Berlin confirm that matched the 1.3 golden. FR3 is vacuously true (no check path). Named open items above are spec/process, not blocking product holes. Party unanimous on this spelling.

## Open questions

- Should empty location, geocoder failure, and a true GeoNames miss keep one stderr line (`No matching place found.`), or split copy in a later story? Spec 1.3 already says failure is the same as no match. A human answer would only change copy, not the epic verdict.
- Confirm tokens: spec lists empty/`y`/`Y` only. Changing that is a spec renegotiation, not a defect.

## Assumptions

- Epic selected from invocation (`ER epic 1`), confirmed by `detect-epic --epic 1`: `story_count` 3, `pending_stories` `[]`, `retro_status` `optional`.
- Yolo treated as headless confirmations: no going-in-concerns prompt; no human verdict override prompt; sprint-status update runs without asking.
- Party requested explicitly, so Phase 3 ran despite the headless “never open the team discussion” default. Party was `--non-interactive` (yolo): one seeded round of independent subagents, then wrap. No HTML keepsake written.
- Criteria treated as **declared** (epics.md FR coverage + story ACs), not profiled.
- Machine verdict **accepted-with-open-items** recorded with no human decision at the console.
- Proposed action items listed above; none auto-applied to specs or code.
- No previous-retro `--set-action-status` transitions to propose.
