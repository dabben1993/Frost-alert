---
scope: Frost Alert single-user frost-warning automation (CAP-1..7)
purpose: build-substrate for coding agents
altitude: feature
updated: 2026-09-13T14:07
---

- (direction) Coaching path. Spine is the only deliverable: a build-substrate for coding agents to start implementing the spec.
- (constraint) Inherit spec: GHA scheduled workflows; Open-Meteo primary + MET Norway fallback (elevation required); Telegram Bot API with cron-polled getUpdates; healthchecks.io dead-man; secrets in GitHub Secrets never source.
- (constraint) Inherit spec: single-user clone+config; no accounts; no always-on personal host; forecast overnight lows only; seasonal ack not nightly snooze; geocode once at setup.
- (constraint) Spec stack.md left language/runtime unlocked. Telegram PoC exists in Python (scripts/poc-telegram-getupdates.py) but is not a stack lock.
- (note) Product state machines, failure modes, and alert-copy split are binding from spec companions; spine must not re-decide them.
- (event) Workspace created at planning-artifacts/architecture/architecture-Frost-alert-2026-09-12. Altitude: feature. Purpose: build-substrate.
- (decision) Paradigm: ports and adapters (hexagonal). Domain owns classification, escalation window, seasonal state. Ports only for what the spec already implies: ForecastSource, Notifier, AckInbox, Watchdog, ConfigStore, Clock. No framework. Prevents setup/check/ack jobs from inventing separate domain rules.
- (direction by user) Accepted ports-and-adapters lean as the system shape.
- (version) Web-checked 2026-09-13: CPython latest stable 3.14.7 (2026-08-05); 3.13.15 also current. actions/setup-python@v7 README example pins python-version 3.13 and says always set version explicitly.
- (version) Web-checked 2026-09-13: uv current line is 0.12.x (0.12.13 reported 2026-09-10). Repo already uses uv for BMAD scripts.
- (decision) Starter: Python 3.13 + uv + stdlib HTTP. Pin 3.13 in .python-version and actions/setup-python@v7 (minor pin, patch floats). No web framework. No requests/httpx unless a port later needs them. Prevents agents inventing Node/Go or a server the spec rejected.
- (direction by user) Accepted Python 3.13 + uv + stdlib HTTP lean.
- (decision) Shared data: committed repo files, split config vs runtime state. Setup writes config only. Runtime state (season, last alert window, getUpdates offset) mutated only via the store port. Secrets never in files. Prevents cache/artifact loss over a seasonal idle and two agents inventing two stores.
- (decision) Single writer: one scheduled workflow runs check then ack poll sequentially and writes state once per run. Setup is local CLI, not a concurrent writer. Prevents lost updates from two jobs committing state.json.
- (direction by user) Accepted repo-file store plus one sequential workflow writer.
- (decision) Window identity: remaining lead time is overnight-low timestamp minus clock-port now, mapped to 24h/12h/6h. Already-alerted is keyed by frost event + window, not workflow name. Prevents late GHA runs sending the wrong cadence step.
- (decision) Watchdog ping only after a successful check: forecast obtained (primary or fallback) and classification finished. Dual-API miss or crash must not ping (CAP-7). Telegram send failure still pings — that is not a missed job. Resolves stack.md vs state-machines.md ping-first conflict in favor of stack.md.
- (note) On finalize, offer to patch spec state-machines.md so the flowchart pings after successful check, not first.
- (direction by user) Accepted remaining-time windows and ping-on-successful-check.
- (direction by user) Rejected night-only classification. Notify before temperature drops to/below threshold even if the first crossing is daytime.
- (decision) Classification uses every forecast hour, not a 20:00-08:00 night window. Risk is present when any upcoming hour is at or below the user threshold. Remaining lead time is first_at_or_below minus clock now. Prevents missing a daytime drop; supersedes the overnight-only lean.
- (note) On finalize, offer to patch spec CAP-2/state-machines/alert-copy language from overnight-low/frost-night to first hour at or below threshold.
- (decision) Frost event = local calendar date (config IANA zone) of the first upcoming hour at or below threshold. Lookahead ~30h. Windows 24/12/6 keyed on that date; same-day forecast revision does not reset. After that date ends, the next crossing is a new event. Prevents a cold snap going silent after the first 6h while plants are still out.
- (direction by user) Keep checking and alerting on later crossings until the user presses the ack button that plants are in. Silence means Suspended, not already-yelled.
- (constraint) Monitoring continues scheduled classify+alert until CAP-4 ack. Suspended still runs the job for watchdog; no frost alerts.
- (direction by user) Simple Telegram in/out: command or a second button that plants are out again. May move plants in and out several times in autumn; not a once-until-spring door.
- (decision) Season flip is Telegram-only: ack control (plants in / Suspended) and resume control (plants out / Monitoring) via command or inline button. Explicit press each time; not a nightly snooze and not auto-resume. CLI stays for setup (CAP-1), not required to flip season. Prevents agents inventing state.json edits or a laptop-only spring ritual.
- (constraint) In/out may repeat in one autumn. Each transition is an explicit Telegram action. Domain still only has Monitoring and Suspended — no snooze-until-tomorrow state.
- (decision) One scheduled workflow every 6 hours: classify, alert if needed, poll Telegram in/out, write state once, ping watchdog on success. Prevents hourly overlap risk and 12h-too-coarse misses of the 6h window and same-afternoon resume.
- (direction by user) Accepted 6-hour workflow cadence.
- (decision) Workflow concurrency: one in-flight run, cancel-in-progress. Newer run has latest committed state. Prevents two writers when GHA delay overlaps the next 6h tick.
- (decision) healthchecks.io: period 6h, grace ~6h, documented next to the ping-URL secret, not hardcoded. workflow_dispatch enabled for a manual tick after an in/out press.
- (direction by user) Accepted cancel-in-progress, 6h+6h watchdog window, and workflow_dispatch.
- (decision) Forecast failover is availability only: Open-Meteo first, 15s timeout; MET Norway on timeout, connect error, 5xx, 429, or empty/malformed hourly. No fallback on 4xx-bad-request. Both fail: no ping, non-zero exit. Domain sees one hourly series; never blend sources.
- (constraint) MET Norway adapter: pass elevation; lat/lon 4 decimals; identifying User-Agent (app name + contact URL/email). Web-checked 2026-09-13: missing/generic UA returns 403 (api.met.no ToS).
- (direction by user) Accepted Open-Meteo-then-MET-Norway availability failover rules.
- (decision) Layout: src/frost_alert/{domain,ports,adapters,entrypoints}; config/user.json; data/state.json; .github/workflows/check.yml. Domain imports no adapters and no HTTP. Adapters implement ports. Entrypoints wire. Unit tests use fake ports; no live network.
- (decision) On-disk format is JSON (stdlib only). Instants UTC ISO-8601; frost-event key is local YYYY-MM-DD. Telegram commands /in and /out match the two buttons.
- (direction by user) Accepted package tree, JSON store, and /in /out commands.
- (decision) Temperatures in domain and on disk are Celsius. scale is setup+display only; setup converts F to threshold_c. Prevents F threshold compared to C forecast hours.
- (decision) config/user.json keys: place_name, lat, lon, elevation_m, timezone, threshold_c, scale (C|F). data/state.json keys: season (monitoring|suspended), event_date (local YYYY-MM-DD|null), alerted_windows (24|12|6), telegram_offset, updated_at UTC. Callback data plants_in/plants_out; always answerCallbackQuery. Logs to stdout.
- (decision) Deferred: exact alert sentences (alert-copy.md owns the split), extra pytest plugins, state-commit message wording.
- (direction by user) Accepted Celsius-on-disk, JSON key sets, callback data, stdout logs. Distill the spine.
- (event) Coaching complete; starting distill.
- (event) Reconcile: folded geocode-once, no live temp, default threshold_c 3, getUpdates/no-webhook/24h, Actions failure email, alert-copy convention, Android battery README, AWS deferred. Intentional spec drift remains: overnight-low wording vs any-hour classification.
- (version) Web-checked 2026-09-13: Telegram Bot API 10.3 released 2026-08-24. Stack pin updated.
- (event) Reviewer gate: lint clean; applied clear fixes from rubric, versions, and adversarial reviews. Added AD-13. No remaining blockers.
- (event) spine finalized
- (decision) AD-13 tick order: load, classify, send then mark, poll, write state, persist via git with contents write permission, then ping. Out command clears event and windows. Forecast series is UTC t plus temp_c. Config and state key sets are AD-3 rules.
- (event) Human walkthrough written: architecture-walkthrough.html. Editorial polish: frost-before-tick order; dropped wobble and read-cursor jargon; keyboard hint added. Spine unchanged.
