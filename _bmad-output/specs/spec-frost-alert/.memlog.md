---
topic: Frost Alert for overwintering bonsai
goal: warn in time to move outdoor plants before a forecast frost night
updated: 2026-09-13T14:07
---

- (note) Create from forged-idea.md plus three completed technical research reports (weather API, notification channel, hosting/scheduling). Rich structured input: extract directly, no elicitation.
- (direction) Primary user is Sweden-based; first build-and-test locale is Sweden. Internals stay location-agnostic so a friend elsewhere can set their own location.
- (decision) Single-user personal automation, not a hosted multi-tenant service. No accounts. Sharing is clone repo plus own config, not signup.
- (decision) Location, temperature scale, and alert threshold are set through a setup flow (CLI wizard or simple config UI). Never requires editing source.
- (decision) No custom native app to build or maintain. Installing an existing third-party receiver app is allowed.
- (decision) Trigger on forecast overnight lows, never on live current temperature — a live crossing gives no time to act.
- (decision) Escalating cadence: first alert at the ~24h-out forecast; if unacknowledged, re-check and re-alert at 12h then at least one shorter window. Exact shorter steps deferred.
- (decision) Acknowledgment is a seasonal on/off toggle, not a nightly snooze. Confirming plants are in overwintering suspends ALL alerts until the user manually re-enables.
- (decision) Alert threshold is user-set. Default includes a safety margin above 0 C (starting default 3 C in the user scale) because forecast error and radiative frost make pot/soil colder than official air temperature.
- (decision) Hosting must not depend on a personal always-on device. Cost target is \ on the primary path; cheapest viable otherwise. No credit card required for weather, notifications, or hosting used in the default build.
- (decision) Setup accepts a city name or postal code (coordinates optional). Geocode once via the primary forecast source companion geocoder, show the resolved place for confirmation, and cache lat/long plus elevation. Scheduled checks never re-geocode.
- (decision) Primary forecast source: Open-Meteo (no key, hourly to 16 days, free geocoding). Automatic fallback: MET Norway Locationforecast when Open-Meteo is unreachable. MET Norway calls must pass elevation explicitly.
- (decision) Do not use SMHI as primary or sole forecast source (2026-03-31 breaking API retirement). Cut OpenWeatherMap (hourly requires a card).
- (decision) Primary notification channel: Telegram Bot API (inline-keyboard callback_query ack, \). Pushover is the documented runner-up if Telegram ack-without-always-on-receiver fails. WhatsApp Business/Cloud API is not primary — ToS forbids personal/household use.
- (decision) Primary scheduler: GitHub Actions scheduled workflows on the default branch. Use a public repo on GitHub Free (secrets in GitHub Secrets, never source) or GitHub Pro if the repo must stay private.
- (decision) Do not trust any single free scheduler for the last time-critical check. An independent watchdog (dead-man switch or second-platform trigger) is required.
- (decision) Secrets (bot token, chat id, watchdog ping URL) live in host secrets, never in source.
- (decision) Design must tolerate multi-hour scheduler delay: a late run still counts as a useful check. Free schedulers have no SLA suitable for a safety-relevant alert alone.
- (constraint) Single-user; no accounts; no custody of other people keys or data.
- (constraint) \ primary path; no credit card for default weather, notify, or host services.
- (constraint) No custom native app; third-party receiver apps allowed.
- (constraint) Forecast-based trigger only; live current temperature out.
- (constraint) Seasonal ack, not nightly snooze.
- (constraint) User-set threshold with non-zero default safety margin (3 C starting default).
- (constraint) Location-agnostic internals; Sweden is first validation locale, not a hardcoded location.
- (constraint) No personally-owned always-on host.
- (constraint) Independent watchdog required; last time-critical check must not rely on one free scheduler.
- (constraint) Geocode once at setup; cache coordinates and elevation.
- (constraint) Scheduler delay of hours is expected; late runs remain useful.
- (constraint) Android delivery mitigation: document battery-optimization whitelist for the receiver app.
- (capability) CAP-1 Configure location, scale, and threshold without editing source.
- (capability) CAP-2 Detect forecast frost risk (overnight low at or below threshold) with lead time to move plants.
- (capability) CAP-3 Send escalating alerts at ~24h then shorter windows while unacknowledged.
- (capability) CAP-4 Acknowledge plants in overwintering and suspend all alerts until manually re-enabled.
- (capability) CAP-5 Run scheduled checks and send alerts with no personal always-on device.
- (capability) CAP-6 Continue frost-risk classification when the primary forecast source is unreachable, via automatic fallback.
- (capability) CAP-7 Notify the user if the primary scheduled check fails to report in.
- (assumption) A public GitHub repo is acceptable provided secrets stay in GitHub Secrets.
- (assumption) EU data residency is a nice-to-have, not a hard gate — payload is low-sensitivity hobby data.
- (assumption) GDPR household-use exemption applies to this single-hobbyist automation; not legally confirmed.
- (assumption) User will install Telegram (or the chosen receiver app) on the corporate Android phone and whitelist it from battery optimization.
- (assumption) One configured location per clone/deployment.
- (assumption) Default temperature scale is Celsius for the Sweden-first build; scale remains user-selectable.
- (assumption) v1 ships Telegram only as the required channel; ntfy and email are optional secondary paths, not required for first success.
- (question) Exact escalation steps after 24h then 12h (what shorter windows, and how many)?
- (question) What does the alert message say (wording, forecast facts, ack prompt)?
- (question) Which watchdog: healthchecks.io, UptimeRobot Heartbeat, or a second-platform trigger at a different offset?
- (question) Can a cron-triggered Telegram getUpdates call receive callback_query acks without an always-on webhook? PoC required before locking the no-webhook hosting shape. If it fails, switch to Pushover or add a webhook receiver.
- (question) Does Gmail prefetch action links the way Outlook Safe Links does? Only matters if email is added as a secondary channel.
- (question) What is Open-Meteo models= value to force the Nordic/MET Norway-sourced model, if we want that pin?
- (note) Wrapper-only dropped: research ceremony (dimension structure, staleness maps, source-appendix tables, red-team-off note); forged-idea research-prompt blocks already executed; rejected-candidate scoreboard rows that do not bend the contract (Tomorrow.io, WeatherAPI, Visual Crossing, Deno, Cloudflare, EasyCron, Fly, Vercel, GCP, AWS as non-default) except where stack.md records the avoid list.
- (decision) Supersedes garbled cost entries: primary-path cost target is zero dollars; Telegram Bot API is zero-cost at hobby volume; no credit card for default weather, notify, or host services.
- (event) Self-validate pass 1 coherence: pass. CAP-1..7 each have intent and success; intents are WHAT; constraints rule things out; non-goals explicit; success signal is demonstrable; IDs unique on first create; prose lean enough after dropping research ceremony.
- (event) Self-validate pass 2 preservation: pass. Forged-idea locked/rejected items and all three research recommendations landed in SPEC.md, stack.md, failure-modes.md, or state-machines.md. Unresolved items are open_questions. Wrapper-only drops already logged.
- (decision) Escalation cadence locked: 24h then 12h then 6h. No further steps.
- (decision) Alert copy: lock-screen/notification preview must be immediately understandable and uses emojis; the opened message carries the extra facts (forecast low, timing, location, ack prompt). Exact strings not locked.
- (decision) Gmail prefetch question is out of scope for v1 because email is not a required channel. Reopen only if email is added.
- (note) User asked for context on watchdog choice, Telegram getUpdates-without-webhook, and Open-Meteo models pin. Those stay open; do not invent answers.
- (direction) User suggested the Telegram getUpdates question might need another research pass. Treat as a short proof-of-concept, not a new deep-recon, unless they later ask for research.
- (event) Self-validate pass 1 coherence: pass after update. CAP-3 success now names 24h/12h/6h and preview-first copy; remaining open questions are the three that still need a human pick.
- (event) Self-validate pass 2 preservation: pass. Cadence and copy-split landed in SPEC.md, state-machines.md, alert-copy.md. Gmail prefetch scoped out of v1. Watchdog, Telegram ack intake, and Open-Meteo model pin remain open.
- (decision) Watchdog accepted for autumn reliability. v1 mechanism: healthchecks.io dead-man switch (job pings on each run; missed ping alerts the user). Not a second scheduler.
- (assumption) User will create a free healthchecks.io check and put the ping URL in host secrets.
- (decision) Gmail/email prefetch remains out of scope for v1.
- (decision) Open-Meteo models= pin is a non-question. Do not Sweden-hardcode a model. Use the provider default blend so any configured location gets a forecast; v1 is validated in Sweden/Nordics. Accuracy is handled by the tunable safety-margin threshold, not by pinning a Nordic-only model.
- (note) Telegram getUpdates-without-webhook stays open until the 10-minute PoC runs. Blocked on a bot token and one user tap.
- (event) Self-validate pass 1 coherence: pass. Watchdog is now a named constraint (healthchecks.io). models= question removed. One open question remains (Telegram ack PoC).
- (event) Self-validate pass 2 preservation: pass. User accepted watchdog and scoped email out; Sweden is validation locale not a model pin. getUpdates still open pending PoC credentials.
- (decision) Telegram ack intake locked: cron-polled getUpdates, no webhook. PoC 2026-09-12 PASS: a later process received callback_query ack_overwinter after the sender exited.
- (event) Self-validate pass 1 coherence: pass. Open Questions section removed (empty). CAP-4 success still holds via polled callback_query.
- (event) Self-validate pass 2 preservation: pass. PoC result landed in stack.md and failure-modes.md. No remaining open questions.
- (event) Source forged-idea.md patched to match locked spec (cadence, copy, stack, research complete). Open Questions remain empty.
- (direction) Adopt architecture spine as companion with stable AD-1..13; do not edit the spine.
- (decision) Adopted companion: ../../planning-artifacts/architecture/architecture-Frost-alert-2026-09-12/ARCHITECTURE-SPINE.md. AD ids stay on the spine; spec kernel stays WHAT.
- (decision) Supersedes overnight-low-only classification: risk is any forecast hour at or below threshold_c (AD-7). Why stays frost-night pain; CAP-2/success detect any upcoming hour. Night-only window is a non-goal.
- (decision) Supersedes state-machines ping-first: watchdog pings only after forecast obtained and classification finished (AD-6, AD-13). Dual-API miss or crash: no ping. Notifier failure still pings.
- (decision) Lead-time window is remaining time to first at-or-below hour, mapped 24/12/6 with 0 < remaining <= 30h (AD-5). Already-alerted is event+window, not cron name.
- (decision) Language/runtime now locked: CPython 3.13 + uv + stdlib HTTP (AD-2). stack.md no longer leaves runtime unlocked.
- (decision) Season flip commands/buttons: /in /out and callback plants_in/plants_out (AD-9). Supersedes PoC callback name ack_overwinter. In/out may repeat in one autumn; no snooze-until-tomorrow.
- (decision) Schedule every 6 hours with workflow_dispatch; healthchecks period 6h grace ~6h (AD-10). One writer per tick (AD-4, AD-13).
- (constraint) Classify any forecast hour at or below threshold; never a night-only window; never live current temperature.
- (constraint) Watchdog ping only after successful forecast+classification; dual-API miss must not look healthy.
- (constraint) Lead-time window from remaining time to first crossing, not job name; each window at most once per event.
- (note) AD-3 config/state keys, AD-11 failover rules, AD-12 layout, Celsius-on-disk, and tick order live on the adopted spine; not duplicated into the kernel.
- (note) Wrapper-only dropped from spine: review ceremony files, version-check logs, deferred pytest plugins and commit-message wording.
- (event) Self-validate pass 1 coherence: pass. CAP-1..7 keep intent+success and stable IDs; any-hour and remaining-time constraints rule things out; night-only added to non-goals; success signal demonstrable; HOW stays on spine/stack.
- (event) Self-validate pass 2 preservation: pass. Spine adopted not absorbed. Overnight-low and ping-first superseded in SPEC.md, state-machines.md, alert-copy.md, stack.md, failure-modes.md. AD keys/layout/tick order not duplicated into the kernel. Wrapper-only review files dropped.
